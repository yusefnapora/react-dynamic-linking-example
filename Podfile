# Uncomment this line to define a global platform for your project
platform :ios, '8.0'
use_frameworks!

target 'RNDynamicLinking' do
  react_source = {
    git: 'https://github.com/yusefnapora/react-native.git',
    branch: 'dynamic-library-modules'
  }
  
  pod 'React', react_source
  pod 'React/RCTText', react_source
end
